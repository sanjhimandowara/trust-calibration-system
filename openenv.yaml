name: trust-calibration-openenv
version: "1.0.7"

entrypoint:
  module: api.server
  app: app

tasks:
  - id: easy
    grader: /grader
  - id: medium
    grader: /grader
  - id: hard
    grader: /grader

api:
  health: /health
  tasks: /tasks
  grader: /grader
  reset: /reset
  step: /step
  state: /state
